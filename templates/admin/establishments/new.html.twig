{% extends 'base_admin.html.twig' %}

{% block title %}Créer une entreprise{% endblock %}

{% block body %}
    <section class="py-5">
        <div class="container-fluid">
            <div class="row align-items-center justify-content-between">
                <div class="col-lg-10">
                    <h1 class="display-1">
                        Créer une entreprise
                    </h1>
                </div>
                <div class="col-lg-2">
                    <div class="d-grid gap-2">
                        <a class="btn btn-secondary-custom" title="Retour vers la liste" href="{{ path('establishments_index') }}">
                            <i class="bi bi-list-ul"></i>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    {{ include('admin/establishments/_form.html.twig') }}

{% endblock %}
